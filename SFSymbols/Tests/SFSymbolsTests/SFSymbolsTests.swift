import XCTest
@testable import SFSymbols

final class SFSymbolsTests: XCTestCase {
    func testExample() throws {
        
//        let symbol = SFSymbol.info
//            .applying(scale: .large)
//            .applying(pointSize: 12, weight: .bold)
//            .applying(textStyle: .body)
    }
}
